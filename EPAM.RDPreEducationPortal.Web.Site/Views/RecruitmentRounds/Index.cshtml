@model IEnumerable<EPAM.RDPreEducationPortal.Web.Site.EntityFramework.Recruitment_Rounds>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoundName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoundKey)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoundName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoundKey)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoundID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoundID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoundID })
        </td>
    </tr>
}

</table>
